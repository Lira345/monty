#include "monty.h"

/**
 * f_stack - prints the element of the top stack
 * @head: A double pointer to the stack head
 * @counter: Pointer to line number
 * Return: no return (void)
 */
void f_stack(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifi = 0;
}
